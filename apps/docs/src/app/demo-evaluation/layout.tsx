/**
 * Evaluation demo layout — full-screen canvas, no docs shell. Theme comes from the root Providers.
 */
export default function DemoEvaluationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
