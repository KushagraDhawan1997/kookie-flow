/**
 * Demo layout - renders children without DocsShell for full-screen canvas.
 * Theme is inherited from root layout's Providers.
 */
export default function DemoLayout({ children }: { children: React.ReactNode }) {
  return children;
}
