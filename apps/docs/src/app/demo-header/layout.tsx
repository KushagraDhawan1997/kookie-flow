/**
 * Header demo layout — full-screen board, no docs shell. Theme comes from the root Providers.
 */
export default function DemoHeaderLayout({ children }: { children: React.ReactNode }) {
  return children;
}
